<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youyu.dao.PostCollectDao">

    <resultMap type="com.youyu.entity.PostCollect" id="PostCollectMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="postId" column="post_id" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="userIdTo" column="user_id_to" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="PostCollectMap">
        select
          id, post_id, user_id, user_id_to, create_time, update_time
        from bs_post_collect
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="PostCollectMap">
        select
          id, post_id, user_id, user_id_to, create_time, update_time
        from bs_post_collect
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="postId != null">
                and post_id = #{postId}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="userIdTo != null">
                and user_id_to = #{userIdTo}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from bs_post_collect
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="postId != null">
                and post_id = #{postId}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="userIdTo != null">
                and user_id_to = #{userIdTo}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into bs_post_collect(post_id, user_id, user_id_to, create_time, update_time)
        values (#{postId}, #{userId}, #{userIdTo}, #{createTime}, #{updateTime})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bs_post_collect(post_id, user_id, user_id_to, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.postId}, #{entity.userId}, #{entity.userIdTo}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bs_post_collect(post_id, user_id, user_id_to, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.postId}, #{entity.userId}, #{entity.userIdTo}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
        on duplicate key update
        post_id = values(post_id),
        user_id = values(user_id),
        user_id_to = values(user_id_to),
        create_time = values(create_time),
        update_time = values(update_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update bs_post_collect
        <set>
            <if test="postId != null">
                post_id = #{postId},
            </if>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="userIdTo != null">
                user_id_to = #{userIdTo},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from bs_post_collect where id = #{id}
    </delete>

</mapper>

