<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youyu.dao.ColumnDao">

    <resultMap type="com.youyu.entity.user.Column" id="ColumnMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="cover" column="cover" jdbcType="VARCHAR"/>
        <result property="status" column="status" jdbcType="VARCHAR"/>
        <result property="isTop" column="is_top" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="deleted" column="deleted" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ColumnMap">
        select
          id, user_id, title, content, cover, status, is_top, create_time, update_time, deleted
        from bs_column
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ColumnMap">
        select
          id, user_id, title, content, cover, status, is_top, create_time, update_time, deleted
        from bs_column
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="title != null and title != ''">
                and title = #{title}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="cover != null and cover != ''">
                and cover = #{cover}
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
            <if test="isTop != null and isTop != ''">
                and is_top = #{isTop}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
            <if test="deleted != null">
                and deleted = #{deleted}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from bs_column
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="title != null and title != ''">
                and title = #{title}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="cover != null and cover != ''">
                and cover = #{cover}
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
            <if test="isTop != null and isTop != ''">
                and is_top = #{isTop}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
            <if test="deleted != null">
                and deleted = #{deleted}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into bs_column(user_id, title, content, cover, status, is_top, create_time, update_time, deleted)
        values (#{userId}, #{title}, #{content}, #{cover}, #{status}, #{isTop}, #{createTime}, #{updateTime}, #{deleted})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bs_column(user_id, title, content, cover, status, is_top, create_time, update_time, deleted)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.userId}, #{entity.title}, #{entity.content}, #{entity.cover}, #{entity.status}, #{entity.isTop}, #{entity.createTime}, #{entity.updateTime}, #{entity.deleted})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bs_column(user_id, title, content, cover, status, is_top, create_time, update_time, deleted)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.title}, #{entity.content}, #{entity.cover}, #{entity.status}, #{entity.isTop}, #{entity.createTime}, #{entity.updateTime}, #{entity.deleted})
        </foreach>
        on duplicate key update
        user_id = values(user_id),
        title = values(title),
        content = values(content),
        cover = values(cover),
        status = values(status),
        is_top = values(is_top),
        create_time = values(create_time),
        update_time = values(update_time),
        deleted = values(deleted)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update bs_column
        <set>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="cover != null and cover != ''">
                cover = #{cover},
            </if>
            <if test="status != null and status != ''">
                status = #{status},
            </if>
            <if test="isTop != null and isTop != ''">
                is_top = #{isTop},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="deleted != null">
                deleted = #{deleted},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from bs_column where id = #{id}
    </delete>

</mapper>

